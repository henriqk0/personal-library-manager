<form method="post" action="{{ path('app_status_delete', {'id': status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this read?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ status.id) }}">
    <button class="btn">Delete</button>
</form>