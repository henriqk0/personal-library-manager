{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
  <section id="create-section" class="content-section active">
    <div class="page-header">
      <h1 class="page-title">Add a new Book</h1>
    </div>

    <div class="md-card">
      {% if form is defined %}
        {{ include('book/_form.html.twig',  {'form': form, 'button_label': 'Add'} ) }}
      {% else %}
      {% endif %}
    </div>
  </section>

  {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
    <script>
      function goBackCreate() {
        window.location.href = "{{ path('app_book_index') }}"
      }
    </script>
  {% endif %}
{% endblock %}